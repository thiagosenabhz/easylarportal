// app/admin/page.tsx
export default function AdminPage() {
  return (
    <main className="min-h-screen flex items-center justify-center bg-slate-50">
      <p className="text-sm text-slate-700">
        Painel do administrador – em implementação. Esta página é apenas um
        placeholder temporário e não interfere no portal público.
      </p>
    </main>
  );
}
